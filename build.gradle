apply plugin: 'groovy'
apply plugin: 'maven-publish'

apply from: "$rootDir/version.gradle"

dependencies {
    compile gradleApi()
    compile localGroovy()
}

publishing {
    publications {
        gitRepoPlugin(MavenPublication) {
            groupId "com.layer"
            artifactId 'git-repo-plugin'
            from components.java
        }
    }
    repositories {
        maven {
            // TODO use this plugin to publish itself
            url "file:///Users/drapp/.gitRepos/layerhq/releases-gradle/releases"
        }
    }
}
